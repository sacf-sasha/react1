import Profil from "./Profil";
import lide from "./data"

/*function App(props){
    return (
        <>
        {
            lide.map(clovek=>
                <Profil
                    id={clovek.id}
                    img={clovek.img}
                    vek={clovek.vek}
                    mesto={clovek.mesto}
                    jmeno={clovek.jmeno}
                    prijmeni={clovek.prijmeni}
                />
            )
        }
        </>
    );
}
export default App*/
